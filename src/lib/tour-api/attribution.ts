// ─── 출처 표기 ────────────────────────────────────────────────────────────────
// 한국관광공사 OpenAPI 이용 요강. 텍스트만 쓴다 — 로고 이미지는 쓸 수 없고
// "TourAPI" 단독 표기도 안 된다. UI 문구는 영어로 쓰지만 이 표기만은
// 규정된 한글 문구를 그대로 둔다.
//
// "한국관광공사"·"KTO"를 브랜딩 요소로 쓰지 않는다. 이 줄이 유일한 용처다.
//
// import 를 하나도 두지 않는 것이 이 파일이 따로 있는 이유다. 쓰는 쪽이 둘 다
// 클라이언트 컴포넌트라, client.ts 나 queries.ts 에 얹으면 TOUR_API_KEY 를 읽는
// 코드로 가는 import 경로가 생긴다.

export const TOUR_API_ATTRIBUTION = "출처: ⓒ한국관광공사";
